apiVersion: v1
kind: Service
metadata:
  name: creativeforge-web-service
  namespace: creativeforge
  labels:
    app: creativeforge-web
spec:
  selector:
    app: creativeforge-web
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: LoadBalancer
